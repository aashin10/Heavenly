export interface Value {
  icon: string;
  title: string;
  description: string;
}

export interface Stat {
  number: string;
  label: string;
}

export interface AboutStream {
  icon: string;
  title: string;
  description: string;
}
