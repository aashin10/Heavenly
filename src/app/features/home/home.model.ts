export interface Domain {
  name: string;
  icon: string;
}

export interface Feature {
  icon: string;
  title: string;
  description: string;
}

export interface ServiceIcon {
  id: string;
  name: string;
  icon: string;
}
